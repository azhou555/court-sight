# Core CV
opencv-python>=4.9
numpy>=1.26
scipy>=1.12

# Object detection + tracking
ultralytics>=8.2        # YOLOv8
boxmot==20.0.0          # pinned: >=21 dropped the boxmot.trackers.BotSort re-export

# Pose estimation: yolo26m-pose.pt (ultralytics, below) replaced RTMPose/mmpose
# to avoid the mmpose/mmcv/mmengine install stack. See src/pipeline/pose/extractor.py.

# Deep learning
torch>=2.2
torchvision>=0.17
ultralytics>=8.4       # YOLO26 support

# Gradient boosted trees
xgboost>=2.0
lightgbm>=4.3
scikit-learn>=1.4

# Data processing
pandas>=2.2
pyarrow>=15.0

# Explainability
shap>=0.45

# Video I/O
yt-dlp>=2024.1          # broadcast footage download
ffmpeg-python>=0.2

# Visualization
matplotlib>=3.8
seaborn>=0.13
pillow>=10.2

# Dev / testing
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
mypy>=1.9

# Dataset access (dev only — not needed at inference time)
huggingface_hub>=0.22
datasets>=2.19
gdown>=5.1
